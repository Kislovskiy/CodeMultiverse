# What is Python's Pass by Assignment?

In Python, "pass by assignment" (also called "pass by object reference") describes how arguments are passed to functions.
It's Python's unique approach that combines aspects of both pass-by-value and pass-by-reference.
After the talk you'll learn the difference between them and leave withan improved skillset.
